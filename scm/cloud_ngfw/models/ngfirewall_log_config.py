# coding: utf-8

"""
    Cloud NGFW

    These APIs are used for reading and managing Cloud NGFW firewall resources on AWS.

    The version of the OpenAPI document: 2.0.0
    Contact: support@paloaltonetworks.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class NgfirewallLogConfig(BaseModel):
    """
    Log destination configuration for the firewall.
    """ # noqa: E501
    account_id: Optional[StrictStr] = Field(default=None, description="The AWS account ID where logs are sent.", alias="AccountId")
    log_destination: Optional[StrictStr] = Field(default=None, description="The ARN or name of the log destination resource.", alias="LogDestination")
    log_destination_region: Optional[StrictStr] = Field(default=None, description="The AWS region of the log destination.", alias="LogDestinationRegion")
    log_destination_type: Optional[StrictStr] = Field(default=None, description="The type of log destination.", alias="LogDestinationType")
    log_type: Optional[List[StrictStr]] = Field(default=None, description="The types of logs to send to this destination.", alias="LogType")
    role_type: Optional[StrictStr] = Field(default=None, description="The IAM role type used to publish logs.", alias="RoleType")
    __properties: ClassVar[List[str]] = ["AccountId", "LogDestination", "LogDestinationRegion", "LogDestinationType", "LogType", "RoleType"]

    @field_validator('log_destination_type')
    def log_destination_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['S3', 'CloudWatchLogs', 'KinesisDataFirehose']):
            raise ValueError("must be one of enum values ('S3', 'CloudWatchLogs', 'KinesisDataFirehose')")
        return value

    @field_validator('log_type')
    def log_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in set(['TRAFFIC', 'DECRYPTION', 'THREAT', 'USER_ID']):
                raise ValueError("each list item must be one of ('TRAFFIC', 'DECRYPTION', 'THREAT', 'USER_ID')")
        return value

    @field_validator('role_type')
    def role_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['ResourceBased', 'IamBased']):
            raise ValueError("must be one of enum values ('ResourceBased', 'IamBased')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of NgfirewallLogConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of NgfirewallLogConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "AccountId": obj.get("AccountId"),
            "LogDestination": obj.get("LogDestination"),
            "LogDestinationRegion": obj.get("LogDestinationRegion"),
            "LogDestinationType": obj.get("LogDestinationType"),
            "LogType": obj.get("LogType"),
            "RoleType": obj.get("RoleType")
        })
        return _obj


