# coding: utf-8

"""
    Objects

    These APIs are used for defining and managing policy object configurations within Strata Cloud Manager.

    The version of the OpenAPI document: 2.0.0
    Contact: support@paloaltonetworks.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class HttpServerProfilesServerInner(BaseModel):
    """
    HttpServerProfilesServerInner
    """ # noqa: E501
    address: Optional[StrictStr] = Field(default=None, description="HTTP server address")
    certificate_profile: Optional[StrictStr] = Field(default='None', description="HTTP server certificate profile")
    http_method: Optional[StrictStr] = Field(default=None, description="HTTP operation to perform")
    name: Optional[StrictStr] = Field(default=None, description="HTTP server name")
    port: Optional[StrictInt] = Field(default=None, description="HTTP server port")
    protocol: Optional[StrictStr] = Field(default=None, description="HTTP server protocol")
    tls_version: Optional[StrictStr] = Field(default=None, description="HTTP server TLS version")
    __properties: ClassVar[List[str]] = ["address", "certificate_profile", "http_method", "name", "port", "protocol", "tls_version"]

    @field_validator('http_method')
    def http_method_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['GET', 'POST', 'PUT', 'DELETE']):
            raise ValueError("must be one of enum values ('GET', 'POST', 'PUT', 'DELETE')")
        return value

    @field_validator('protocol')
    def protocol_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['HTTP', 'HTTPS']):
            raise ValueError("must be one of enum values ('HTTP', 'HTTPS')")
        return value

    @field_validator('tls_version')
    def tls_version_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['1.0', '1.1', '1.2', '1.3']):
            raise ValueError("must be one of enum values ('1.0', '1.1', '1.2', '1.3')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of HttpServerProfilesServerInner from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of HttpServerProfilesServerInner from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "address": obj.get("address"),
            "certificate_profile": obj.get("certificate_profile") if obj.get("certificate_profile") is not None else 'None',
            "http_method": obj.get("http_method"),
            "name": obj.get("name"),
            "port": obj.get("port"),
            "protocol": obj.get("protocol"),
            "tls_version": obj.get("tls_version")
        })
        return _obj


