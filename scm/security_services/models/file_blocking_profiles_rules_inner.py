# coding: utf-8

"""
    Security Services

    These APIs are used for defining and managing security services configurations within Strata Cloud Manager.

    The version of the OpenAPI document: 2.0.0
    Contact: support@paloaltonetworks.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class FileBlockingProfilesRulesInner(BaseModel):
    """
    FileBlockingProfilesRulesInner
    """ # noqa: E501
    action: StrictStr = Field(description="The action to take when the rule match criteria is met")
    application: Annotated[List[StrictStr], Field(min_length=1)] = Field(description="The application transferring the files (App-ID naming)")
    direction: StrictStr = Field(description="The direction of the file transfer")
    file_type: Annotated[List[StrictStr], Field(min_length=1)] = Field(description="The file type")
    name: StrictStr = Field(description="The name of the file blocking rule")
    __properties: ClassVar[List[str]] = ["action", "application", "direction", "file_type", "name"]

    @field_validator('action')
    def action_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['alert', 'block', 'continue']):
            raise ValueError("must be one of enum values ('alert', 'block', 'continue')")
        return value

    @field_validator('application')
    def application_validate_enum(cls, value):
        """Validates the enum"""
        for i in value:
            if i not in set(['any', 'axifile', 'dl-free', 'facebook-mail', 'file.io', 'giphy-base', 'glassdoor-uploading', 'http-proxy', 'redbooth', 'send-anywhere', 'zoho-mail']):
                raise ValueError("each list item must be one of ('any', 'axifile', 'dl-free', 'facebook-mail', 'file.io', 'giphy-base', 'glassdoor-uploading', 'http-proxy', 'redbooth', 'send-anywhere', 'zoho-mail')")
        return value

    @field_validator('direction')
    def direction_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['download', 'upload', 'both']):
            raise ValueError("must be one of enum values ('download', 'upload', 'both')")
        return value

    @field_validator('file_type')
    def file_type_validate_enum(cls, value):
        """Validates the enum"""
        for i in value:
            if i not in set(['any', '7z', 'chm', 'hlp', 'bat', 'class', 'dll', 'hta', 'jar', 'pif', 'vbe', 'wsf']):
                raise ValueError("each list item must be one of ('any', '7z', 'chm', 'hlp', 'bat', 'class', 'dll', 'hta', 'jar', 'pif', 'vbe', 'wsf')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of FileBlockingProfilesRulesInner from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of FileBlockingProfilesRulesInner from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "action": obj.get("action") if obj.get("action") is not None else 'alert',
            "application": obj.get("application"),
            "direction": obj.get("direction") if obj.get("direction") is not None else 'both',
            "file_type": obj.get("file_type"),
            "name": obj.get("name")
        })
        return _obj


