# coding: utf-8

"""
    Security Services

    These APIs are used for defining and managing security services configurations within Strata Cloud Manager.

    The version of the OpenAPI document: 2.0.0
    Contact: support@paloaltonetworks.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from scm_security_services.models.decryption_rules_type import DecryptionRulesType
from typing import Optional, Set
from typing_extensions import Self

class DecryptionRules(BaseModel):
    """
    DecryptionRules
    """ # noqa: E501
    action: StrictStr = Field(description="The action to be taken")
    category: List[StrictStr] = Field(description="The destination URL category")
    description: Optional[StrictStr] = Field(default=None, description="The description of the decryption rule")
    destination: List[StrictStr] = Field(description="The destination addresses")
    destination_hip: Optional[List[StrictStr]] = Field(default=None, description="The Host Integrity Profile of the destination host")
    device: Optional[Annotated[str, Field(strict=True, max_length=64)]] = Field(default=None, description="The device in which the resource is defined")
    disabled: Optional[StrictBool] = Field(default=None, description="Is the rule disabled?")
    folder: Optional[Annotated[str, Field(strict=True, max_length=64)]] = Field(default=None, description="The folder in which the resource is defined")
    var_from: List[StrictStr] = Field(description="The source security zone", alias="from")
    id: Optional[StrictStr] = Field(default=None, description="The UUID of the decryption rule")
    log_fail: Optional[StrictBool] = Field(default=None, description="Log failed decryption events?")
    log_setting: Optional[StrictStr] = Field(default=None, description="The log settings of the decryption rule")
    log_success: Optional[StrictBool] = Field(default=None, description="Log successful decryption events?")
    name: StrictStr = Field(description="The name of the decryption rule")
    negate_destination: Optional[StrictBool] = Field(default=None, description="Negate the destination addresses?")
    negate_source: Optional[StrictBool] = Field(default=None, description="Negate the source addresses?")
    profile: Optional[StrictStr] = Field(default=None, description="The decryption profile associated with the decryption rule")
    service: List[StrictStr] = Field(description="The destination services and/or service groups")
    snippet: Optional[Annotated[str, Field(strict=True, max_length=64)]] = Field(default=None, description="The snippet in which the resource is defined")
    source: List[StrictStr] = Field(description="The source addresses")
    source_hip: Optional[List[StrictStr]] = None
    source_user: List[StrictStr] = Field(description="List of source users and/or groups.  Reserved words include `any`, `pre-login`, `known-user`, and `unknown`.")
    tag: Optional[List[StrictStr]] = Field(default=None, description="The tags associated with the decryption rule")
    to: List[StrictStr] = Field(description="The destination security zone")
    type: Optional[DecryptionRulesType] = None
    __properties: ClassVar[List[str]] = ["action", "category", "description", "destination", "destination_hip", "device", "disabled", "folder", "from", "id", "log_fail", "log_setting", "log_success", "name", "negate_destination", "negate_source", "profile", "service", "snippet", "source", "source_hip", "source_user", "tag", "to", "type"]

    @field_validator('action')
    def action_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['decrypt', 'no-decrypt']):
            raise ValueError("must be one of enum values ('decrypt', 'no-decrypt')")
        return value

    @field_validator('device')
    def device_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^[a-zA-Z\d\-_\. ]+$", value):
            raise ValueError(r"must validate the regular expression /^[a-zA-Z\d\-_\. ]+$/")
        return value

    @field_validator('folder')
    def folder_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^[a-zA-Z\d\-_\. ]+$", value):
            raise ValueError(r"must validate the regular expression /^[a-zA-Z\d\-_\. ]+$/")
        return value

    @field_validator('snippet')
    def snippet_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^[a-zA-Z\d\-_\. ]+$", value):
            raise ValueError(r"must validate the regular expression /^[a-zA-Z\d\-_\. ]+$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DecryptionRules from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * OpenAPI `readOnly` fields are excluded.
        """
        excluded_fields: Set[str] = set([
            "id",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of type
        if self.type:
            _dict['type'] = self.type.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DecryptionRules from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "action": obj.get("action"),
            "category": obj.get("category"),
            "description": obj.get("description"),
            "destination": obj.get("destination"),
            "destination_hip": obj.get("destination_hip"),
            "device": obj.get("device"),
            "disabled": obj.get("disabled"),
            "folder": obj.get("folder"),
            "from": obj.get("from"),
            "id": obj.get("id"),
            "log_fail": obj.get("log_fail"),
            "log_setting": obj.get("log_setting"),
            "log_success": obj.get("log_success"),
            "name": obj.get("name"),
            "negate_destination": obj.get("negate_destination"),
            "negate_source": obj.get("negate_source"),
            "profile": obj.get("profile"),
            "service": obj.get("service"),
            "snippet": obj.get("snippet"),
            "source": obj.get("source"),
            "source_hip": obj.get("source_hip"),
            "source_user": obj.get("source_user"),
            "tag": obj.get("tag"),
            "to": obj.get("to"),
            "type": DecryptionRulesType.from_dict(obj["type"]) if obj.get("type") is not None else None
        })
        return _obj


