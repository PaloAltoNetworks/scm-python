# coding: utf-8

"""
    Network Services

    These APIs are used for defining and managing network services configuration within Strata Cloud Manager.

    The version of the OpenAPI document: 2.0.0
    Contact: support@paloaltonetworks.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from scm_network_services.models.nat_rules_destination_translation import NatRulesDestinationTranslation
from scm_network_services.models.nat_rules_dynamic_destination_translation import NatRulesDynamicDestinationTranslation
from scm_network_services.models.nat_rules_source_translation import NatRulesSourceTranslation
from typing import Optional, Set
from typing_extensions import Self

class NatRules(BaseModel):
    """
    NatRules
    """ # noqa: E501
    active_active_device_binding: Optional[StrictStr] = None
    description: Optional[StrictStr] = Field(default=None, description="NAT rule description")
    destination: List[StrictStr] = Field(description="Destination address(es) of the original packet")
    destination_translation: Optional[NatRulesDestinationTranslation] = None
    device: Optional[Annotated[str, Field(strict=True, max_length=64)]] = Field(default=None, description="The device in which the resource is defined")
    disabled: Optional[StrictBool] = Field(default=False, description="Disable NAT rule?")
    dynamic_destination_translation: Optional[NatRulesDynamicDestinationTranslation] = None
    folder: Optional[Annotated[str, Field(strict=True, max_length=64)]] = Field(default=None, description="The folder in which the resource is defined")
    var_from: List[StrictStr] = Field(description="Source zone(s) of the original packet", alias="from")
    id: StrictStr = Field(description="UUID of the resource")
    name: StrictStr = Field(description="NAT rule name")
    nat_type: Optional[StrictStr] = Field(default='ipv4', description="NAT type")
    service: StrictStr = Field(description="The service of the original packet")
    snippet: Optional[Annotated[str, Field(strict=True, max_length=64)]] = Field(default=None, description="The snippet in which the resource is defined")
    source: List[StrictStr] = Field(description="Source address(es) of the original packet")
    source_translation: Optional[NatRulesSourceTranslation] = None
    tag: Optional[List[StrictStr]] = Field(default=None, description="NAT rule tags")
    to: List[StrictStr] = Field(description="Destination zone of the original packet")
    to_interface: Optional[StrictStr] = Field(default=None, description="Destination interface of the original packet")
    __properties: ClassVar[List[str]] = ["active_active_device_binding", "description", "destination", "destination_translation", "device", "disabled", "dynamic_destination_translation", "folder", "from", "id", "name", "nat_type", "service", "snippet", "source", "source_translation", "tag", "to", "to_interface"]

    @field_validator('active_active_device_binding')
    def active_active_device_binding_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['primary', 'both', '0', '1']):
            raise ValueError("must be one of enum values ('primary', 'both', '0', '1')")
        return value

    @field_validator('device')
    def device_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^[a-zA-Z\d\-_\. ]+$", value):
            raise ValueError(r"must validate the regular expression /^[a-zA-Z\d\-_\. ]+$/")
        return value

    @field_validator('folder')
    def folder_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^[a-zA-Z\d\-_\. ]+$", value):
            raise ValueError(r"must validate the regular expression /^[a-zA-Z\d\-_\. ]+$/")
        return value

    @field_validator('nat_type')
    def nat_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['ipv4', 'nat64', 'nptv6']):
            raise ValueError("must be one of enum values ('ipv4', 'nat64', 'nptv6')")
        return value

    @field_validator('snippet')
    def snippet_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^[a-zA-Z\d\-_\. ]+$", value):
            raise ValueError(r"must validate the regular expression /^[a-zA-Z\d\-_\. ]+$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of NatRules from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * OpenAPI `readOnly` fields are excluded.
        """
        excluded_fields: Set[str] = set([
            "id",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of destination_translation
        if self.destination_translation:
            _dict['destination_translation'] = self.destination_translation.to_dict()
        # override the default output from pydantic by calling `to_dict()` of dynamic_destination_translation
        if self.dynamic_destination_translation:
            _dict['dynamic_destination_translation'] = self.dynamic_destination_translation.to_dict()
        # override the default output from pydantic by calling `to_dict()` of source_translation
        if self.source_translation:
            _dict['source_translation'] = self.source_translation.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of NatRules from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "active_active_device_binding": obj.get("active_active_device_binding"),
            "description": obj.get("description"),
            "destination": obj.get("destination"),
            "destination_translation": NatRulesDestinationTranslation.from_dict(obj["destination_translation"]) if obj.get("destination_translation") is not None else None,
            "device": obj.get("device"),
            "disabled": obj.get("disabled") if obj.get("disabled") is not None else False,
            "dynamic_destination_translation": NatRulesDynamicDestinationTranslation.from_dict(obj["dynamic_destination_translation"]) if obj.get("dynamic_destination_translation") is not None else None,
            "folder": obj.get("folder"),
            "from": obj.get("from"),
            "id": obj.get("id"),
            "name": obj.get("name"),
            "nat_type": obj.get("nat_type") if obj.get("nat_type") is not None else 'ipv4',
            "service": obj.get("service"),
            "snippet": obj.get("snippet"),
            "source": obj.get("source"),
            "source_translation": NatRulesSourceTranslation.from_dict(obj["source_translation"]) if obj.get("source_translation") is not None else None,
            "tag": obj.get("tag"),
            "to": obj.get("to"),
            "to_interface": obj.get("to_interface")
        })
        return _obj


